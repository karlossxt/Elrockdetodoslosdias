<section class="playlists-cta">
    <div class="cta-container">
        <div class="cta-content">
            <span class="cta-tag">CURADURÍA SONORA</span>
            <h2>EL RUIDO NO SE DETIENE: <br/> <span class="outline">ESCUCHA NUESTRAS PLAYLISTS</span></h2>
            <p>Selecciones exclusivas diseñadas por nuestro equipo editorial para acompañar cada estado de ánimo.</p>
            <a href="/playlists" class="btn-playlist-go">
                EXPLORAR CATÁLOGO <i class="fa-brands fa-spotify"></i>
            </a>
        </div>
    </div>
</section>

<style>
    .playlists-cta {
        background: #000;
        padding: 100px 5%;
        border-top: 1px solid #111;
        overflow: hidden;
        position: relative;
    }

    /* Fondo decorativo con movimiento sutil */
    .playlists-cta::before {
        content: "";
        position: absolute;
        top: 0; right: 0; width: 40%; height: 100%;
        background: linear-gradient(to left, #ff000022, transparent);
        pointer-events: none;
    }

    .cta-container { max-width: 1400px; margin: 0 auto; }
    .cta-tag { font-family: 'Inter'; font-weight: 900; font-size: 0.7rem; color: var(--accent); letter-spacing: 4px; }
    
    h2 { 
        font-family: 'Bebas Neue'; 
        font-size: 6rem; 
        line-height: 0.85; 
        margin: 20px 0; 
        color: white;
    }
    .outline { color: transparent; -webkit-text-stroke: 1.5px white; }

    p { 
        font-family: 'Inter'; 
        color: #666; 
        font-size: 1.2rem; 
        max-width: 550px; 
        margin-bottom: 40px; 
        font-weight: 500;
    }

    .btn-playlist-go {
        display: inline-flex;
        align-items: center;
        gap: 15px;
        background: #1DB954; /* Verde Spotify */
        color: black;
        padding: 20px 40px;
        text-decoration: none;
        font-family: 'Bebas Neue';
        font-size: 1.8rem;
        border-radius: 4px;
        transition: 0.3s;
    }

    .btn-playlist-go:hover {
        background: white;
        transform: translateY(-5px) scale(1.05);
    }

    @media (max-width: 768px) {
        h2 { font-size: 3rem; }
        .btn-playlist-go { font-size: 1.3rem; padding: 15px 30px; }
    }
</style>