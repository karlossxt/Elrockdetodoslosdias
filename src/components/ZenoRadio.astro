<section class="radio-live">
    <div class="container">
        <div class="radio-box">
            <div class="radio-info">
                <span class="live-tag"><span class="dot"></span> EN VIVO</span>
                <h2>RADIO <span class="outline">ONLINE</span></h2>
                <p>Escucha la mejor selección de rock las 24 horas del día.</p>
            </div>
            <div class="player-container">
                <!-- AQUÍ PEGA TU CÓDIGO DE ZENO.FM -->
                <!-- Ejemplo de cómo suele ser el iframe de Zeno: -->
                <iframe 
                    src="https://zeno.fm/player/el-rock-de-todos-los-dias" 
                    width="100%" 
                    height="100" 
                    frameborder="0" 
                    scrolling="no">
                </iframe>
            </div>
        </div>
    </div>
</section>

<style>
    .radio-live { padding: 40px 5%; background: #000; }
    .container { max-width: 1400px; margin: 0 auto; }
    .radio-box { 
        background: #0a0a0a; 
        border: 2px solid var(--accent); 
        padding: 30px; 
        display: flex; 
        align-items: center; 
        justify-content: space-between;
        gap: 40px;
    }
    .radio-info { flex: 1; }
    .live-tag { color: var(--accent); font-family: 'Bebas Neue'; letter-spacing: 2px; display: flex; align-items: center; gap: 10px; margin-bottom: 10px; }
    .dot { width: 10px; height: 10px; background: var(--accent); border-radius: 50%; animation: pulse 1.5s infinite; }
    h2 { font-size: 4rem; margin: 0; line-height: 0.9; }
    .outline { color: transparent; -webkit-text-stroke: 1px white; }
    p { color: #666; font-weight: 600; margin-top: 10px; }

    .player-container { flex: 1.5; background: #111; border-radius: 4px; overflow: hidden; height: 100px; }

    @keyframes pulse { 0% { opacity: 1; } 50% { opacity: 0.3; } 100% { opacity: 1; } }
    
    @media (max-width: 900px) {
        .radio-box { flex-direction: column; text-align: center; }
        .live-tag { justify-content: center; }
        h2 { font-size: 3rem; }
    }
</style>