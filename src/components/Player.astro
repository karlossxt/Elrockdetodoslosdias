<div class="rock-player">
    <div class="player-content">
        <div class="track-info">
            <img src="https://picsum.photos/seed/album/100" />
            <div class="details">
                <span class="song">Entre Dos Tierras</span>
                <span class="artist">Héroes del Silencio</span>
            </div>
            <button class="like"><i class="fa-regular fa-heart"></i></button>
        </div>

        <div class="controls">
            <div class="buttons">
                <button><i class="fa-solid fa-backward-step"></i></button>
                <button class="main-play"><i class="fa-solid fa-play"></i></button>
                <button><i class="fa-solid fa-forward-step"></i></button>
            </div>
            <div class="progress-bar">
                <span>1:45</span>
                <div class="bar"><div class="fill"></div></div>
                <span>4:22</span>
            </div>
        </div>

        <div class="volume-area">
            <i class="fa-solid fa-volume-high"></i>
            <div class="vol-bar"><div class="vol-fill"></div></div>
            <span class="live-badge">LIVE</span>
        </div>
    </div>
</div>

<style>
    .rock-player { position: fixed; bottom: 0; left: 0; width: 100%; background: rgba(5,5,5,0.9); backdrop-filter: blur(20px); border-top: 1px solid #222; padding: 15px 0; z-index: 2000; }
    .player-content { max-width: 1400px; margin: 0 auto; display: grid; grid-template-columns: 1fr 2fr 1fr; align-items: center; padding: 0 20px; }
    
    .track-info { display: flex; align-items: center; gap: 15px; }
    .track-info img { width: 50px; height: 50px; border-radius: 5px; }
    .song { display: block; font-weight: bold; font-size: 14px; }
    .artist { font-size: 12px; color: #888; }
    .like { background: none; border: none; color: #555; cursor: pointer; }

    .controls { display: flex; flex-direction: column; align-items: center; gap: 8px; }
    .buttons { display: flex; align-items: center; gap: 30px; color: #888; }
    .main-play { background: white; color: black; width: 40px; height: 40px; border-radius: 50%; border: none; display: flex; align-items: center; justify-content: center; }

    .progress-bar { display: flex; align-items: center; gap: 10px; width: 100%; font-size: 10px; color: #555; }
    .bar { flex: 1; height: 4px; background: #222; border-radius: 2px; }
    .fill { width: 30%; height: 100%; background: var(--accent); }

    .volume-area { display: flex; align-items: center; justify-content: flex-end; gap: 15px; }
    .vol-bar { width: 80px; height: 3px; background: #222; }
    .vol-fill { width: 70%; height: 100%; background: #888; }
    .live-badge { background: rgba(255,0,0,0.1); color: var(--accent); border: 1px solid var(--accent); padding: 2px 8px; font-size: 10px; font-weight: 900; }

    @media (max-width: 768px) { .player-content { grid-template-columns: 1fr; } .volume-area, .progress-bar { display: none; } }
</style>