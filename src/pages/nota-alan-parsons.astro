---
import RockLayout from '../layouts/RockLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Player from '../components/Player.astro';

const DATES = [
  { city: 'MONTERREY', date: '05 ABRIL 2025', venue: 'ESCENARIO GNP', seller: 'TICKETMASTER', color: '#ff0000' },
  { city: 'PUEBLA', date: '06 ABRIL 2025', venue: 'EXPLANADA PUEBLA', seller: 'BLACK TICKET', color: '#fff' },
  { city: 'CDMX', date: '09 ABRIL 2025', venue: 'ARENA CDMX', seller: 'SUPERBOLETOS', color: '#ff0000' }
];
---

<RockLayout title="Alan Parsons Live Project: Tour México | El Rock de Todos los Días">
    <Header />

    <article class="tour-article">
        <header class="tour-header">
            <div class="container">
                <span class="category">AGENDA // PROGRESIVO</span>
                <h1>ALAN PARSONS <br/> <span class="outline">LIVE PROJECT</span></h1>
                <p class="tour-name">TOUR MÉXICO 2025: EL MAESTRO SONORO REGRESA</p>
            </div>
        </header>

        <div class="tour-grid">
            <!-- COLUMNA IZQUIERDA: INFORMACIÓN -->
            <div class="tour-info">
                <img src="https://images.unsplash.com/photo-1514525253361-bee8718a74a2?q=80&w=800" alt="Alan Parsons" class="main-photo" />
                
                <div class="bio-snippet">
                    <p>Alan Parsons, el ganador del Grammy y una de las figuras más influyentes de la música progresiva, visitará México para ofrecer tres conciertos exclusivos.</p>
                    <p>Conocido por su trabajo como productor en álbumes como <strong>Abbey Road</strong> de The Beatles y <strong>The Dark Side of the Moon</strong> de Pink Floyd, Parsons celebra 50 años de trayectoria.</p>
                </div>

                <div class="social-box">
                    <span>Sigue a la leyenda:</span>
                    <div class="icons">
                        <a href="#"><i class="fa-brands fa-spotify"></i></a>
                        <a href="#"><i class="fa-brands fa-instagram"></i></a>
                        <a href="#"><i class="fa-brands fa-facebook"></i></a>
                    </div>
                </div>
            </div>

            <!-- COLUMNA DERECHA: FECHAS (ESTILO TICKET) -->
            <div class="tour-dates">
                <h3>FECHAS CONFIRMADAS</h3>
                {DATES.map(show => (
                    <div class="ticket-card">
                        <div class="date-side">
                            <span class="day">{show.date.split(' ')[0]}</span>
                            <span class="month">{show.date.split(' ')[1]}</span>
                        </div>
                        <div class="info-side">
                            <h4>{show.city}</h4>
                            <p>{show.venue}</p>
                            <a href="#" class="buy-btn">COMPRAR EN {show.seller}</a>
                        </div>
                    </div>
                ))}
                
                <div class="promoter-tag">
                    PRESENTADO POR: <span class="red">ARTA PRODUCCIONES</span>
                </div>
            </div>
        </div>
    </article>

    <Footer />
    <Player />
</RockLayout>

<style>
    .tour-article { padding-top: 60px; background: #000; }
    .container { max-width: 1200px; margin: 0 auto; padding: 0 5%; }
    
    .tour-header { text-align: center; margin-bottom: 60px; }
    .category { font-family: 'Bebas Neue'; color: var(--accent); letter-spacing: 3px; font-size: 1.2rem; }
    h1 { font-size: 8rem; line-height: 0.8; margin: 20px 0; }
    .outline { color: transparent; -webkit-text-stroke: 1.5px white; }
    .tour-name { font-family: 'Inter'; font-weight: 800; color: #555; font-size: 1.2rem; }

    .tour-grid { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 60px; padding: 0 5% 100px; }
    
    /* Info Side */
    .main-photo { width: 100%; height: 500px; object-fit: cover; filter: grayscale(100%); border: 1px solid #222; }
    .bio-snippet { margin-top: 30px; font-size: 1.1rem; line-height: 1.6; color: #ccc; }
    .bio-snippet p { margin-bottom: 20px; }

    .social-box { margin-top: 40px; padding-top: 20px; border-top: 1px solid #111; }
    .social-box span { font-family: 'Bebas Neue'; color: #444; font-size: 1.2rem; display: block; margin-bottom: 15px; }
    .icons { display: flex; gap: 20px; font-size: 1.5rem; color: white; }

    /* Dates Side (Ticket Style) */
    .tour-dates h3 { font-size: 3rem; margin-bottom: 30px; }
    .ticket-card { 
        display: flex; 
        background: #0a0a0a; 
        border: 1px solid #1a1a1a; 
        margin-bottom: 20px;
        transition: 0.3s;
    }
    .ticket-card:hover { border-color: var(--accent); transform: translateX(10px); }

    .date-side { 
        background: #111; 
        padding: 20px; 
        display: flex; 
        flex-direction: column; 
        align-items: center; 
        justify-content: center;
        border-right: 2px dashed #222;
        min-width: 80px;
    }
    .day { font-family: 'Bebas Neue'; font-size: 2.5rem; line-height: 1; color: var(--accent); }
    .month { font-family: 'Bebas Neue'; font-size: 1rem; color: white; }

    .info-side { padding: 20px; flex-grow: 1; }
    .info-side h4 { font-family: 'Bebas Neue'; font-size: 2rem; margin: 0; color: white; }
    .info-side p { font-size: 0.9rem; color: #666; margin: 5px 0 15px; font-weight: bold; }
    
    .buy-btn { 
        display: inline-block; 
        background: #fff; 
        color: #000; 
        padding: 8px 15px; 
        font-family: 'Bebas Neue'; 
        font-size: 1.1rem; 
        text-decoration: none;
        transition: 0.3s;
    }
    .buy-btn:hover { background: var(--accent); color: white; }

    .promoter-tag { margin-top: 40px; font-family: 'Bebas Neue'; color: #333; font-size: 1.2rem; }
    .red { color: var(--accent); }

    @media (max-width: 900px) {
        .tour-grid { grid-template-columns: 1fr; }
        h1 { font-size: 4rem; }
    }
</style>