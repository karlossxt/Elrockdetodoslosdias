---
const { title } = Astro.props;
---
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body class="rock-body">
    <slot /> 
    
    <style is:global>
    :root {
        --bg: #000000;
        --accent: #ff0000; /* El rojo rockero */
        --gray-text: #666666;
    }

    /* Dentro de <style is:global> en RockLayout.astro */
/* ANIMACIÓN DE REVELADO */
    .reveal {
        opacity: 0;
        transform: translateY(30px);
        transition: all 0.8s cubic-bezier(0.2, 1, 0.3, 1);
    }

    .reveal.active {
        opacity: 1;
        transform: translateY(0);
    }

    /* Retrasos para grids (opcional) */
    .delay-1 { transition-delay: 0.1s; }
    .delay-2 { transition-delay: 0.2s; }
    .delay-3 { transition-delay: 0.3s; }
h1, h2, h3 {
    font-family: 'Bebas Neue', sans-serif;
    text-transform: uppercase;
}

.section-title {
    font-size: 5rem;
    line-height: 0.85;
    letter-spacing: -1px;
}

.red-italic {
    color: #ff0000;
    font-style: italic;
}

/* Tipografía de cuerpo */
p, span, a, div {
    font-family: 'Inter', sans-serif;
}

    /* La barrita roja vertical que ves en tus imágenes */
    .red-bar-vertical {
        width: 6px;
        height: 60px;
        background: var(--accent);
        flex-shrink: 0;
    }

    /* Los badges grises de las fotos */
    .gray-badge {
        background: rgba(255, 255, 255, 0.05);
        color: #888;
        padding: 5px 12px;
        font-size: 10px;
        font-weight: 800;
        border-radius: 4px;
        text-transform: uppercase;
    }

    /* Grid estándar para que todo alinee igual */
    .main-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 30px;
    }

    @media (max-width: 1100px) { .main-grid { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 600px) { .main-grid { grid-template-columns: 1fr; } }
</style>
</body>
</html>